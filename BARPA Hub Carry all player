-- [[ BARPA HUB SECURITY SYSTEM v1.8 ]]
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local GroupService = game:GetService("GroupService")
local LocalPlayer = Players.LocalPlayer

-- SETTINGS
local webhookURL = "https://discord.com/api/webhooks/1428603744320360508/76OMF6gQK7gj6o3CZSRRB6nrGNs-Ui6LfPVl7Pj_LsKlh56i8U6AAVCOR0865dmRiw8A" 
local GroupId = 35010872 
local cacheBuster = "?t=" .. os.time()
local configURL = "https://raw.githubusercontent.com/jellanberdinardo/luca/main/config.json" .. cacheBuster

-- 1. KILL SWITCH & GROUP CHECK (Security Gate)
local successConfig, configRes = pcall(function() return game:HttpGet(configURL) end)
if successConfig then
    local config = HttpService:JSONDecode(configRes)
    if config.IsActive == false then
        LocalPlayer:Kick("\n[BARPA HUB]\nDisabled by Luca_doncicz.")
        return 
    end
end

if not LocalPlayer:IsInGroup(GroupId) then
    pcall(function() GroupService:PromptJoinAsync(GroupId) end)
    LocalPlayer:Kick("\n[BARPA HUB]\nJoin the group to use this script!")
    return
end

-- 2. LOG SUCCESS
local function logExec()
    local proxyURL = webhookURL:gsub("discord.com", "webhook.lewisakura.moe")
    pcall(function()
        HttpService:PostAsync(proxyURL, HttpService:JSONEncode({
            ["embeds"] = {{["title"] = "BARPA HUB LOADED", ["description"] = LocalPlayer.Name .. " is using the script!", ["color"] = 65280}}
        }))
    end)
end
logExec()

-- ==========================================
-- [[ PASTE YOUR CARRY ALL GUI CODE BELOW ]]
-- ==========================================

-- EXAMPLE (Delete this and paste YOUR actual GUI code here):
local sg = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
local frame = Instance.new("Frame", sg)
frame.Size = UDim2.new(0, 200, 0, 100)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
local text = Instance.new("TextLabel", frame)
text.Text = "BARPA HUB ACTIVE"
text.Size = UDim2.new(1, 0, 1, 0)
text.TextColor3 = Color3.new(1, 1, 1)

-- ==========================================
