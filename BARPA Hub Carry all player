-- [[ BARPA HUB v8.7 - CRASH-PROOF LOGIC ]]
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local GroupService = game:GetService("GroupService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

-- [[ 1. CONFIGURATION ]]
local GroupId = 35010872 
local configURL = "https://raw.githubusercontent.com/jellanberdinardo/luca/main/config.json?t=" .. os.time()

-- [[ 2. SMART SECURITY CHECK ]]
local function runSecurity()
    -- We use pcall to prevent the script from crashing if your JSON has a typo
    local success, result = pcall(function()
        local data = game:HttpGet(configURL)
        return HttpService:JSONDecode(data)
    end)
    
    -- If the JSON is fixed and loaded correctly
    if success and result then
        -- ONLY kick if IsActive is EXPLICITLY set to false
        if result.IsActive == false or result.IsActive == "false" then
            local kickMsg = result.Reason or "Disabled by Luca_doncicz."
            -- If you forgot to change the reason while disabling, it shows this:
            if kickMsg == "System is active." then
                kickMsg = "Maintenance in progress."
            end
            
            LocalPlayer:Kick("\n[BARPA HUB]\n" .. kickMsg)
            return false
        end
    else
        -- If your JSON has an error (like an extra comma), it prints a warning 
        -- but DOES NOT kick you anymore.
        warn("[BARPA] JSON Error detected! Check your config.json for extra commas.")
    end
    
    -- Group Lock
    if not LocalPlayer:IsInGroup(GroupId) then
        pcall(function() GroupService:PromptJoinAsync(GroupId) end)
        LocalPlayer:Kick("\n[BARPA HUB]\nPlease join the group to use this script!")
        return false
    end
    
    return true
end

-- Run security
if not runSecurity() then return end

-- [[ 3. THE GUI ]]
local ScreenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
ScreenGui.Name = "BARPA_V8_7"
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame", ScreenGui)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Main.Position = UDim2.new(0.5, -150, 0.4, 0)
Main.Size = UDim2.new(0, 300, 0, 180)
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", Main).Color = Color3.fromRGB(0, 200, 200)

local TopBar = Instance.new("Frame", Main)
TopBar.Size = UDim2.new(1, 0, 0, 45)
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 10)

local Title = Instance.new("TextLabel", TopBar)
Title.Size = UDim2.new(1, 0, 1, 0)
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Text = "BARPA HUB <font color='#00FFFF'>v8.7</font>"
Title.RichText = true
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Font = Enum.Font.GothamBold
Title.TextSize = 14
Title.BackgroundTransparency = 1

local CarryBtn = Instance.new("TextButton", Main)
CarryBtn.Position = UDim2.new(0, 15, 0, 65)
CarryBtn.Size = UDim2.new(1, -30, 0, 50)
CarryBtn.BackgroundColor3 = Color3.fromRGB(30, 35, 35)
CarryBtn.Text = "RAPID CARRY ALL"
CarryBtn.TextColor3 = Color3.new(0, 1, 1)
CarryBtn.Font = Enum.Font.GothamBold
CarryBtn.TextSize = 14
Instance.new("UICorner", CarryBtn).CornerRadius = UDim.new(0, 8)

-- [[ 4. RAPID LOGIC ]]
CarryBtn.MouseButton1Click:Connect(function()
    local replic = ReplicatedStorage:FindFirstChild("CarryReplic")
    local remote = replic and replic.CarryRemotes:FindFirstChild("CarryRemote")
    local choice = replic and replic.CarryChoices:FindFirstChild("Piggyback")

    if remote and choice then
        CarryBtn.Text = "EXECUTING..."
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                task.spawn(function()
                    remote:FireServer({
                        ["cmd"] = "Carry",
                        ["firstPlr"] = p,
                        ["carrychoicesss"] = choice
                    })
                end)
            end
        end
        task.wait(1)
        CarryBtn.Text = "RAPID CARRY ALL"
    end
end)
